/**
 * v4 canonical visual set (2026-07-18): every marketing slot has its own
 * purpose-built plate in the brand design language (graphite armor, phone
 * face with orange smile). No slot may reuse another slot's image.
 * Registry: docs/product/ASSET-REGISTRY.md §v4
 */

/** Homepage hero — full-body walker, studio floor, no baked text. */
export const EXOBOD_HERO_IMAGE = "/exobod/v4/hero-main.png";

/** Desk One page hero — the actual EVT desk product. */
export const DESK_ONE_HERO_IMAGE = "/exobod/v4/body-desk.png";

/** Story step 01 — four-body family lineup (landscape). */
export const STORY_CHOOSE_BODY = "/exobod/v4/family-lineup.png";

/** Story step 02 — hands docking a phone into the head cradle. */
export const STORY_PHONE_DOCK = "/exobod/v4/phone-dock.png";

/** Story step 03 — walker mid-gesture, motion tuning. */
export const STORY_MOTION = "/exobod/v4/motion-tuning.png";

/** Story step 04 — QC workbench with calipers and open unit. */
export const STORY_ORDER_QC = "/exobod/v4/qc-bench.png";

/** Body selector tiles — one unique plate per body type. */
export const BODY_WALKER = "/exobod/v4/body-walker.png";
export const BODY_DESK = "/exobod/v4/body-desk.png";
export const BODY_ROVER = "/exobod/v4/body-rover.png";
export const BODY_UTILITY = "/exobod/v4/body-utility.png";

/** Product hub — exploded assembly showing the phone as the core (landscape). */
export const PRODUCT_EXPLODED = "/exobod/v4/exploded-build.png";

/** Build system — machined actuator/frame macro detail. */
export const BUILD_MACRO = "/exobod/v4/mechanical-macro.png";

/** Open Graph / social share banner (16:9, copy-safe left third). */
export const OG_BANNER = "/exobod/v4/og-banner.png";
