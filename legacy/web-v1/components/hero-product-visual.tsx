"use client";

import { DeskOneStage } from "@/components/desk-one-stage";

/**
 * Hero product stage — Desk One (real product path), not the walker infographic poster.
 */
export function HeroProductVisual() {
  return (
    <div className="relative mx-auto w-full max-w-md lg:max-w-lg xl:max-w-xl">
      <DeskOneStage />
    </div>
  );
}
